// ---------------------------------------------------------------------------
// PLACEHOLDER CONFIG — swap these two values before launch, nothing else
// needs to change anywhere else in the codebase.
// ---------------------------------------------------------------------------
export const CONFIG = {
  // TODO(placeholder): replace with Luisa's real Instagram profile URL
  INSTAGRAM_URL: 'https://www.instagram.com/PLACEHOLDER_LUANCE/',
  // TODO(placeholder): replace with the real Calendly booking link
  CALENDLY_URL: 'https://calendly.com/PLACEHOLDER_LUANCE/kostenloses-call',
};
